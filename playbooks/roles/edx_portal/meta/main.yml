---
dependencies:
  - role: portal_subsite
    portal__subsites:
      - name: "edx_lms" 
        urlpath: "{{ edx__lms_urlpath }}"
        port: "{{ edx__lms_port }}"
      - name: "edx_cms" 
        urlpath: "{{ edx__cms_urlpath }}"
        port: "{{ edx__cms_port }}"
