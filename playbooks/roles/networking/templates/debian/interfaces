# {{ ansible_managed }}
{% for i in network__interface_configs %}
{{ "# " + i.name if i.name is defined }}
auto {{ i.device }}
  {% if i.address is not defined or i.address == "dhcp" %}
	iface {{ i.device }} inet dhcp
  {% else %}
	iface {{ i.device }} inet static
        address {{ i.address }}
  {% endif %}

{% endfor %}
